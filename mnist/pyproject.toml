[tool.poetry]
name = "mnist"
version = "0.1.0"
description = ""
authors = ["Victor Matekole <victor@matekole.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
